# if selectPeriod_1 == '3':
#    if selectPeriod.dateContract.month >= 1 and selectPeriod.dateContract.month <= selectPeriod.expirationDateContract.month:
#        if selectPeriod.expirationDateContract.month <= 3:
#            return JsonResponse({'data':'3'})
# elif selectPeriod_1 == '6':
#    if selectPeriod.dateContract.month >= 1 and selectPeriod.dateContract.month <= selectPeriod.expirationDateContract.month:
#        if selectPeriod.expirationDateContract.month <= 6:
#            return JsonResponse({'data':'6'})
# elif selectPeriod_1 == '12':
#    if selectPeriod.dateContract.month >=1 and selectPeriod.dateContract.month <= selectPeriod.expirationDateContract.month:
#        if selectPeriod.expirationDateContract.month <= 12:
#            return JsonResponse({'data': '12'})



